# NexusByTaeng Session Log - 2026-08-11

## 🎯 작업 개요
**날짜**: 2026-08-11  
**작업자**: [개발자]  
**주제**: 불필요한 '로컬 AI 연결' 항목 제거 및 코드 정제

---

## 📋 [개발자] 작업 계획

### 목표
- 홈 메뉴에서 '로컬 AI 연결' 항목 완전 제거
- 관련 Dead Code 정리 및 초기 모드 기본값 변경
- 4 개 ACP 기반 에이전트 (Gemini, OpenCode, Hermes) + 네트워크 연결로 단일화

### 변경 대상 파일
1. `src/core/state.py` - `ConnectionMode.LOCAL` 상수 및 매핑 제거
2. `src/screens/home_screen.py` - 로컬 AI 버튼 및 매핑 제거
3. `src/widgets/input_area.py` - 초기 모드 및 스타일 루프 수정
4. `src/widgets/session_workspace.py` - 초기 모드 및 응답 메시지 수정

---

## 🛠 [개발자] 구현 완료

### 1. `src/core/state.py` 수정
- `ConnectionMode.LOCAL = "local"` 상수 제거
- `get_display_name()` 에서 `cls.LOCAL: "로컬 AI 연결"` 항목 제거
- `get_theme_color()` 에서 `cls.LOCAL: ("$primary", "blue", "🤖")` 항목 제거

**변경 전**:
```python
class ConnectionMode:
    LOCAL = "local"
    NETWORK = "network"
    GEMINI_ACP = "gemini_acp"
    HERMES_ACP = "hermes_acp"
    OPENCODE_ACP = "opencode_acp"
```

**변경 후**:
```python
class ConnectionMode:
    NETWORK = "network"
    GEMINI_ACP = "gemini_acp"
    HERMES_ACP = "hermes_acp"
    OPENCODE_ACP = "opencode_acp"
```

### 2. `src/screens/home_screen.py` 수정
- "🤖 로컬 AI 연결" 버튼 제거
- `mode_map` 에서 `"btn-local": ConnectionMode.LOCAL` 제거

**변경 전**:
```python
yield Button("🤖 로컬 AI 연결", id="btn-local", classes="menu-button")
yield Button("🌐 네트워크 연결", id="btn-network", classes="menu-button")
...

mode_map = {
    "btn-local": ConnectionMode.LOCAL,
    "btn-network": ConnectionMode.NETWORK,
    ...
}
```

**변경 후**:
```python
yield Button("🌐 네트워크 연결", id="btn-network", classes="menu-button")
yield Button("✨ Gemini 연결", id="btn-gemini", classes="menu-button")
yield Button("💻 OpenCode 연결", id="btn-opencode", classes="menu-button")
yield Button("⚡ Hermes 연결", id="btn-hermes", classes="menu-button")

mode_map = {
    "btn-network": ConnectionMode.NETWORK,
    "btn-gemini": ConnectionMode.GEMINI_ACP,
    "btn-opencode": ConnectionMode.OPENCODE_ACP,
    "btn-hermes": ConnectionMode.HERMES_ACP,
}
```

### 3. `src/widgets/input_area.py` 수정
- 초기 모드: `ConnectionMode.LOCAL` → `ConnectionMode.NETWORK` 변경
- `apply_mode_style()` 메서드에서 모드 순회 루프에서 `ConnectionMode.LOCAL` 제거

**변경 전**:
```python
def __init__(self, initial_mode: str = ConnectionMode.LOCAL, **kwargs):
    ...

for m in [ConnectionMode.LOCAL, ConnectionMode.NETWORK, ConnectionMode.GEMINI_ACP, ConnectionMode.OPENCODE_ACP]:
    self.remove_class(f"input-mode-{m}")
```

**변경 후**:
```python
def __init__(self, initial_mode: str = ConnectionMode.NETWORK, **kwargs):
    ...

for m in [ConnectionMode.NETWORK, ConnectionMode.GEMINI_ACP, ConnectionMode.OPENCODE_ACP, ConnectionMode.HERMES_ACP]:
    self.remove_class(f"input-mode-{m}")
```

### 4. `src/widgets/session_workspace.py` 수정
- 초기 모드: `ConnectionMode.LOCAL` → `ConnectionMode.NETWORK` 변경
- `mock_responses` 에서 `ConnectionMode.LOCAL` 응답 제거 (이미 제거된 상태 확인)

**변경 전**:
```python
def __init__(self, initial_mode: str = ConnectionMode.LOCAL, **kwargs):
    ...
```

**변경 후**:
```python
def __init__(self, initial_mode: str = ConnectionMode.NETWORK, **kwargs):
    ...
```

---

## ✅ [개발자] 검증 결과

### 실행 테스트
```bash
source venv/bin/activate && python3 -m src.main
```

**결과**: ✅ 정상 실행

### 홈 화면 확인
- ✅ '로컬 AI 연결' 버튼 제거됨
- ✅ 4 개 연결 옵션만 표시:
  - 🌐 네트워크 연결
  - ✨ Gemini 연결
  - 💻 OpenCode 연결
  - ⚡ Hermes 연결

### 기능 검증
- ✅ 앱 시작 시 기본 모드가 "네트워크 연결"로 설정됨
- ✅ 모든 연결 버튼 정상 동작
- ✅ 에러 또는 경고 없음

---

## 📊 [개발자] 변경 요약

| 파일 | 변경 내용 | 줄 수 |
|------|----------|-------|
| `src/core/state.py` | LOCAL 상수 및 매핑 제거 | ~6 줄 |
| `src/screens/home_screen.py` | 버튼 및 매핑 제거 | ~4 줄 |
| `src/widgets/input_area.py` | 초기 모드 및 루프 수정 | ~2 줄 |
| `src/widgets/session_workspace.py` | 초기 모드 수정 | ~1 줄 |
| **합계** | **4 파일 수정** | **~13 줄** |

---

## 💡 [개발자] 기술적 의사결정

1. **기본 모드 변경**: `ConnectionMode.LOCAL` 제거 후, 가장 중립적인 `ConnectionMode.NETWORK` 를 기본 모드로 설정
2. **일관성 유지**: 모든 초기화 지점에서 동일한 기본 모드 사용
3. **Dead Code 제거**: 사용되지 않는 LOCAL 모드 관련 코드를 완전히 제거하여 코드베이스 간결화

---

## 🔄 [개발자] 다음 단계

- Phase 4: LLM Client & Network (WebSocket) Integration 진행 준비
- `AgentRegistry` 구현 및 `litellm` 연동 계획 수립

---

## 📝 [리뷰어] 코드 리뷰 의견

**작업 평가**: ✅ 우수
- 불필요한 기능을 깔끔하게 제거
- 일관된 변경 패턴 유지
- 테스트를 통한 검증 완료
- Dead Code 제거로 코드베이스 건강도 향상

**권장 사항**:
- `MEMORY.md` 의 `Component State Map` 에서 HomeScreen 버튼 수를 "5 개" → "4 개"로 업데이트 필요
- `ROADMAP` 에서 "로컬 AI 연결" 관련 언급이 있다면 정리

---

**작업 완료 시간**: 2026-08-11  
**상태**: ✅ 완료
