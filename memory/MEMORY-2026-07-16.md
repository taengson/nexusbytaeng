# Memory - 2026-07-16

## Objective
Build `nexusbytaeng`, a professional AI-Network Hybrid Chat TUI using `textual` and `litellm`.

## Progress
- Implemented `HomeScreen`, `TabbedContent` workspace layout, and `ProjectTreePanel` sidebar.
- Applied dark theme (`.tcss`) and modularized UI components.
- Fixed layout bug: `ProjectTreePanel` and `TabbedContent` are now wrapped in `#workspace-container` (hidden by default), ensuring the sidebar only appears during active sessions.
- Added `.hidden` CSS class to `src/styles/nexus.tcss` to correctly handle visibility toggling.
- Cleaned `examples/` and updated `.gitignore`.
- Implemented Toad-style interactive input system:
    - Core Logic: `InputDispatcher`, `ShellExecutor`, `CommandHandler`, and `PathSuggester` implemented in `src/core/input_engine.py`.
    - UI Components: Added `ConfirmationModal` for shell transition and `SuggestionPanel` with `ListView` for keyboard-navigable suggestions.
    - Integration: Implemented mode-based CSS switching (Normal, Shell, Command, Suggestion) and state-driven input routing.
- UI/UX Polishing:
    - Fixed `ScreenStackError` in `ConfirmationModal` transition.
    - Implemented `.shell-bubble` for left-aligned shell output.
    - Added floating-style `SuggestionPanel` and permanent input guide `! shell mode, / command`.
    - Resolved Textual CSS parsing errors by removing unsupported properties (`font-size`, `position: absolute`, `z-index`).

## Current State
- Toad-style input features integrated: `!` for shell, `/` for commands. (`@` path suggestions removed per requirement).
- `src/main.py` correctly toggles between `HomeScreen` and the `#workspace-container`.
- Input guide and shell output styling are operational.

## Pending Implementation Plan (Approved for Build)
### 1. File Tree & Input Integration
- Implement event propagation from `ProjectTreePanel` (DirectoryTree) to `SessionWorkspace`.
- Automatically inject selected file path/name into `InputArea` for easier shell usage.

### 2. LLM Client & Network (WebSocket) Integration
- Implement Multi-provider LLM `AgentRegistry` and `litellm` integration.
- Build asynchronous WebSocket broker for network panels.

## Next Steps
- [ ] Implement `ProjectTreePanel` $\rightarrow$ `InputArea` file path injection.
- [ ] Adjust `InputArea` vertical positioning to prevent it from being too high.
- [ ] Implement actual LLM/Network logic for session workspaces.
- [ ] Add more built-in slash commands for workspace management.
